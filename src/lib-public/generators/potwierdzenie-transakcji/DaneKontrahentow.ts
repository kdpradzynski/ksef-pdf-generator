import { Content } from 'pdfmake/interfaces';
import { Podmiot1, Podmiot2 } from '../../types/fa3.types';

export function generateDaneKontrahentow(daneSprzedawcy?: Podmiot1, daneNabywcy?: Podmiot2): Content[] {
  return [];
}
